Rule: react-doctor/no-high-complexity-react-function
Severity: warning
Category: Maintainability
Count: 11

`AgentPanel` has cyclomatic complexity 47, cognitive complexity 56, and maximum nesting depth 2, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`AgentSessionLayout` has cyclomatic complexity 28, cognitive complexity 55, and maximum nesting depth 5, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`DiffToolbar` has cyclomatic complexity 67, cognitive complexity 87, and maximum nesting depth 3, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`DiffContents` has cyclomatic complexity 20, cognitive complexity 26, and maximum nesting depth 3, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`RepositoryPanel` has cyclomatic complexity 49, cognitive complexity 109, and maximum nesting depth 6, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`PerformanceChart` has cyclomatic complexity 35, cognitive complexity 45, and maximum nesting depth 3, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`PerformanceHud` has cyclomatic complexity 31, cognitive complexity 42, and maximum nesting depth 3, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`PullRequestReviewBar` has cyclomatic complexity 34, cognitive complexity 40, and maximum nesting depth 4, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`RepositoryReviewHeader` has cyclomatic complexity 15, cognitive complexity 21, and maximum nesting depth 4, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`RepositoryDiffPanel` has cyclomatic complexity 14, cognitive complexity 16, and maximum nesting depth 3, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

`EditorStatusBar` has cyclomatic complexity 14, cognitive complexity 21, and maximum nesting depth 4, so its React logic is hard to understand and change. Extract independent branches into components or hooks.

Suggestion: Extract independent render branches and state logic into focused components or hooks until the control flow is easy to follow.

Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-high-complexity-react-function

Files:
  src/renderer/src/AgentPanel.tsx:76
  src/renderer/src/App.tsx:364
  src/renderer/src/AppView.tsx:497
  src/renderer/src/DiffSurface.tsx:249
  src/renderer/src/GitHubPanel.tsx:160
  src/renderer/src/PerformanceChart.tsx:46
  src/renderer/src/PerformanceHud.tsx:49
  src/renderer/src/PullRequestReviewBar.tsx:27
  src/renderer/src/RepositoryWorkspace.tsx:274
  src/renderer/src/RepositoryWorkspace.tsx:804
  src/renderer/src/editor/EditorStatusBar.tsx:17
