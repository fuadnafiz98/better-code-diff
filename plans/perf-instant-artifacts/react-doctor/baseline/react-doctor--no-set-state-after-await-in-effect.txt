Rule: react-doctor/no-set-state-after-await-in-effect
Severity: warning
Category: Bugs
Count: 2

This setter runs after `await`, so overlapping re-runs of the effect can resolve out of order and write stale state; gate it behind a cancellation/ignore flag or return a cleanup that cancels the work.

Suggestion: In a `useEffect` whose dependencies can change, guard any setter call that runs after an `await` behind a cancellation/ignore flag, or return a cleanup that cancels the async work.

Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-set-state-after-await-in-effect

Files:
  extensions/horus/src/open-pull-request.tsx:16
  extensions/horus/src/open-pull-request.tsx:28
